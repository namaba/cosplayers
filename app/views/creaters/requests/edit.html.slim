.container
  .card
    .card-body
      = form_with model: [:creater, @request.work], local: true do |f|
        #works.field
          = f.fields_for :photos do |w|
            = render 'work_fields', f: w
          .links
            = link_to_add_association '追加', f, :works
        = f.submit '保存', class: 'btn btn-success btn-raised'